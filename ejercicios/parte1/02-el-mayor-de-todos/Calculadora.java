public class Calculadora {

    /**
     * Devuelve el número más grande del arreglo.
     * Puedes suponer que el arreglo tiene al menos un número.
     */
    public static int mayor(int[] numeros) {
        // TODO: empieza suponiendo que el mayor es el primero, y compara con el resto
        return 0;
    }
}
