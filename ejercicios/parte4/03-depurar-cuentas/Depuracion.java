import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

public class Depuracion {

    /**
     * Elimina de la lista todas las cuentas inactivas y devuelve los códigos
     * de las que eliminó, en el orden en que estaban.
     *
     * Si no hay inactivas, la lista queda igual y se devuelve una lista vacía (no null).
     * Resuélvelo con un Iterator: it.hasNext(), it.next() e it.remove().
     */
    public static List<String> depurarInactivas(List<Cuenta> cuentas) {
        // TODO
        return null;
    }
}
