import java.util.ArrayList;

public class Listas {

    /**
     * Devuelve una lista dinámica con las mismas palabras del arreglo,
     * en el mismo orden.
     *
     * Con un arreglo vacío devuelve una lista vacía (no null).
     */
    public static ArrayList<String> desdeArreglo(String[] palabras) {
        // TODO: crea la lista, recorre el arreglo y ve agregando con add
        return null;
    }
}
