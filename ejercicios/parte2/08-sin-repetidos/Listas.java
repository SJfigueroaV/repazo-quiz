import java.util.ArrayList;

public class Listas {

    /**
     * Devuelve una lista NUEVA con las mismas palabras pero sin repetidos,
     * conservando el orden de la primera aparición.
     *
     * De [sol, luna, sol, mar] devuelve [sol, luna, mar].
     */
    public static ArrayList<String> sinRepetidos(ArrayList<String> palabras) {
        // TODO: el método contains de la lista te ahorra un for entero
        return null;
    }
}
